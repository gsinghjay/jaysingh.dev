<!DOCTYPE html>
<html lang="en">
<head>
  {% include "partials/meta.njk" %}

  <title>{% if title %}{{ title }} | {% endif %}{{ profile.name }}</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="flex flex-col min-h-screen">
  {% include "partials/skip-link.njk" %}

  <header class="bg-white border-b-4 border-black sticky top-0 z-50">
    <div class="max-w-6xl mx-auto px-6 py-4">
      {% include "partials/header.njk" %}
    </div>
  </header>

  <main id="main-content" tabindex="-1" class="max-w-6xl mx-auto px-6 py-8 flex-grow">
    {{ content | safe }}
  </main>

  <footer class="bg-black text-white mt-auto">
    <div class="max-w-6xl mx-auto px-6 py-6">
      {% include "partials/footer.njk" %}
    </div>
  </footer>

  <script src="/js/main.js"></script>
</body>
</html>
