# Test Environment Configuration
# Copy to .env and modify as needed

# Environment: local | staging | production
TEST_ENV=local

# Base URL for tests (defaults to localhost:8080 for 11ty dev server)
BASE_URL=http://localhost:8080
